# 2026-05-08 开发日志

## 上下文恢复
- 换电脑开发，引擎路径从 `C:\Users\Dindo\Documents\repo\UE_5.4` 变为 `D:\Projects\UnrealEngine`（5.4.4-release 源码版）
- 项目路径从 `C:\Users\Dindo\Documents\repo\Unreal Projects\MTBRacing\` 变为 `D:\Projects\MTBRacing\`
- CodeBuddy 内置记忆已清理，后续完全靠 `.workbuddy/memory/` 维持上下文

## 完成事项

### Phase 1 第 2 步：Core 目录骨架 ✅
- 用户手写 `Public/Core/MTBGameMode.h` + `Private/Core/MTBGameMode.cpp`
- `AMTBGameMode` 继承 `AGameModeBase`，空壳构造函数
- 采用 Public/Private 分离目录结构（比平铺更规范）
- 编译通过，Editor 中 World Settings 能看到 MTBGameMode

### Phase 1 第 3 步：启用 ChaosVehicles + BikePawn ✅
- `.uproject` 添加 `ChaosVehiclesPlugin` 插件
- `Build.cs` 添加 `ChaosVehicles` 模块依赖
- 创建 `ABikePawn`（继承 `AWheeledVehiclePawn`）
  - `Public/Pawn/BikePawn.h` + `Private/Pawn/BikePawn.cpp`
  - 包含 `SetupPlayerInputComponent` 和 `Tick` 虚函数 override

### Phase 1 第 4 步：车轮配置 ✅
- 创建 `UBikeFrontWheel`（`Public/Pawn/BikeFrontWheel.h`）
  - WheelRadius=35, WheelWidth=6, MaxSteerAngle=40, AxleType=Front
- 创建 `UBikeRearWheel`（`Public/Pawn/BikeRearWheel.h`）
  - WheelRadius=35, WheelWidth=6, MaxSteerAngle=0, AxleType=Rear, MaxHandBrakeTorque=3000
- `ABikePawn` 构造函数中装配 2 轮到 `VehicleMovement->WheelSetups`

## 当前源码结构

```
Source/MTBRacing/
├── MTBRacing.Build.cs
├── MTBRacing.h / .cpp
├── Public/
│   ├── Core/
│   │   └── MTBGameMode.h
│   └── Pawn/
│       ├── BikePawn.h
│       ├── BikeFrontWheel.h
│       └── BikeRearWheel.h
└── Private/
    ├── Core/
    │   └── MTBGameMode.cpp
    └── Pawn/
        ├── BikePawn.cpp
        ├── BikeFrontWheel.cpp
        └── BikeRearWheel.cpp
```

## 下一步（Phase 1 第 5 步：Enhanced Input）

1. 在 Editor 中创建 Input 资产：
   - `IA_Throttle`（Axis1D）— W/S 键
   - `IA_Steering`（Axis1D）— A/D 键
   - `IA_Handbrake`（Digital）— Space 键
   - `IMC_Bike`（Input Mapping Context）— 绑定上述键位
2. C++ 端在 `ABikePawn` 中：
   - 声明 3 个 `UInputAction*` UPROPERTY 指针
   - `SetupPlayerInputComponent` 中绑定回调
   - 回调函数把输入值传给 `VehicleMovementComponent`

## 协作提醒
- 用户自己写代码，AI 只做指导
- 插件按需加载原则
- 下次继续说"继续 MTB 项目"即可
