# 2026-09-05

## 2.2 UMTBAttributeSet 完成 + Code Review（23:54）

用户自己写完 `Public/Gas/MTBAttributeSet.h` + `Private/Gas/MTBAttributeSet.cpp`，编译通过。

### 破例记录（R1 例外）

用户明确要求"review 时可以把相关知识点写在注释里" → AI 直接改了这两个文件，**只加注释和 TODO 标记，不改任何代码逻辑**。bug 修复留给用户自己动手。

### 写得对的地方

- 四个属性类型全用 `FGameplayAttributeData`（没用裸 float）
- `ATTRIBUTE_ACCESSORS` 自定义宏正确组合了 4 个引擎基础宏
- 四个 `OnRep_` 都有 `UFUNCTION()`，函数体只有 REPNOTIFY 宏（没自己写赋值）
- `DOREPLIFETIME_CONDITION_NOTIFY` 用了 `REPNOTIFY_Always`（预测系统要求）
- `BlueprintReadOnly` 而非 ReadWrite
- 正确没有把 `CurrentSpeed` 做成 Attribute

### 🔴 真 Bug：缺 `PreAttributeBaseChange`（2.5 会爆）

从源码确认两条独立路径：
- CurrentValue 路径 `AttributeSet.cpp:99-102`：`PreAttributeChange` → `SetCurrentValue`
- BaseValue 路径 `GameplayEffect.cpp:3720-3732`：`PreAttributeBaseChange` → `SetBaseValue`

当前只覆写了前者。`GE_StaminaRegen`（Infinite + Period=1.0）是 Periodic GE，按 Instant 处理、改 BaseValue → **Stamina BaseValue 无上限增长**（100→110→...→700）。

表现极具欺骗性：HUD 显示 100/100 正常，但 Boost 扣耐力时要先消耗掉虚高的 BaseValue，表现为"Boost 不掉耐力"然后突然暴跌。从现象反推不到根因。

修法要点：签名结尾**必须有 `const`**（`AttributeSet.h:226`），漏了就不是覆写而是新增函数 —— 编译过但永不被调用，又一个静默失效。

### 🟡 其他建议（已在注释里标 TODO）

1. `PreAttributeChange` 没调 `Super::` —— 5.4 基类是空实现故无功能 bug，但插入中间基类时会静默跳过
2. `MaxStamina = 100.;` 少 f 后缀（double 字面量，功能无碍但不一致）
3. `MaxSpeed` 无钳制 —— 配错的 GE 能让它变负数，车会往后飘
4. `MaxStamina` 变小时 `Stamina` 不跟随收缩（Phase 2 不会触发）
5. 构造函数用 `Stamina = 100.f` 靠 `FGameplayAttributeData(float)` 隐式转换，**能工作且等价于 InitStamina**；推荐 `InitXxx()` 是因为意图明确、不依赖隐式转换

### 目录命名偏差

用户建的是 `Gas/`，而 `mtbracing-rules.md` R3 里写的子目录约定是 `GAS/`。Windows 文件系统大小写不敏感所以能编译。项目只做 Windows，影响可忽略，但与成文约定不一致 —— 待用户决定是改目录还是改 rules 文档。

## 下一步：2.3

ASC 挂 BikePawn（用户已拍板）+ MaxSpeed→Movement 搭桥。
桥必须用 `GetGameplayAttributeValueChangeDelegate`，不能用 `PostGameplayEffectExecute`
（后者对 Infinite GE 改 CurrentValue 不触发，源码注释 `AttributeSet.h:198-201` 明确说明）。
