# 2026-04-20 开发日志

## 上下文恢复
- Phase 0 已完成，Phase 1 开始
- 确认 UE 版本为 **5.4 源码版**，引擎路径 `C:\Users\Dindo\Documents\repo\UE_5.4`
- 工作区路径保持 `C:\Users\Dindo\Documents\repo\Unreal Projects\MTBRacing\`（带空格，不改）

## 约定
- **插件按需加载**：不提前启用 ChaosVehicles / GameplayAbilities，用到哪个再加哪个，减少编译面和冷启动时间
- 项目创建方式：用户手动在 Epic Launcher / UE Editor 创建 Blank C++ 模板

## 下一步
- 等用户手动创建 UE5 C++ 项目壳
- 创建完后由我介入：
  - 检查 `.uproject`、`Source/MTBRacing/MTBRacing.Build.cs`、`Config/` 初始状态
  - 按 DEV_PLAN.md 规划的目录结构搭 C++ 骨架（Core/ Vehicle/ GAS/ Player/ Network/ 子目录）
  - Phase 1 首个里程碑：能跑起来的空项目 + 目录骨架

## 项目壳创建 & 重命名（21:49~22:05）
- 用户手动创建时项目名是 `MTBRacing_tmp`（临时目录名未改回），需整体改名
- 已完成重命名（7 处）：
  - `MTBRacing_tmp.uproject` → `MTBRacing.uproject`
  - `Source/MTBRacing_tmp/` → `Source/MTBRacing/`
  - `Source/MTBRacing_tmp.Target.cs` → `Source/MTBRacing.Target.cs`（类名 `MTBRacingTarget`）
  - `Source/MTBRacing_tmpEditor.Target.cs` → `Source/MTBRacingEditor.Target.cs`（类名 `MTBRacingEditorTarget`）
  - `Source/MTBRacing/MTBRacing.Build.cs`（类名 `MTBRacing`，模块依赖只保留官方默认 + EnhancedInput）
  - `Source/MTBRacing/MTBRacing.h/cpp`（`IMPLEMENT_PRIMARY_GAME_MODULE` 模块名同步）
  - `Config/DefaultEngine.ini` 的 `ActiveGameNameRedirects` 同步改为 `/Script/MTBRacing`
- 按"按需加载"原则，清掉了 `.uproject` 里默认启用的 `ModelingToolsEditorMode` 插件
- 删除了所有 `_tmp` 文件和空目录

## 源码版 UE 注意点（重要）
- 生成 VS 工程文件必须用源码版的 `GenerateProjectFiles.bat`，不能右键 `.uproject`（会走 Launcher 注册的引擎）
- 正确命令：`& "C:\Users\Dindo\Documents\repo\UE_5.4\GenerateProjectFiles.bat" "<.uproject 路径>"`

## 待用户确认
- 运行 GenerateProjectFiles 生成 `.sln`
- VS 中编译 Development Editor | Win64
- 双击 `.uproject` 或 F5 打开 Editor
- 确认项目能跑起来（Phase 1 第 1 步收尾）

## Phase 1 第 1 步完成（22:06）
- 用户报告 Editor 能正常打开，重命名无后遗症
- 模块加载、Config、.uproject 全部 OK

## Phase 1 第 2 步：Core 目录骨架（22:06~）
- 新建 `Source/MTBRacing/Core/MTBGameMode.h/cpp`
- `AMTBGameMode` 只继承 `AGameModeBase`，构造函数空壳，不设默认 Pawn/Controller
- 严格遵守"按需加载"：不建其他空子目录（Vehicle/GAS/Player/Network 等有实际代码时再建）
- 不引入新模块依赖、不启插件
- 类命名规范：`MTBRACING_API` 宏（UHT 根据模块名自动生成，全大写）
- include 路径规范：`#include "Core/MTBGameMode.h"`（相对模块 Source 根，避免未来子目录重名歧义）

## 待用户验证
- VS 编译通过（UBT 自动扫描 Core/ 子目录）
- Editor 里 World Settings → GameMode Override 下拉能看到 `MTBGameMode`

## 22:11 协作模式调整（重要）
- 用户明确要求：所有代码和操作必须由他亲自完成，AI 只做指导
- 已把 AI 协作原则写进 MEMORY.md 顶部
- 之前我已自动生成的 `Core/MTBGameMode.h/cpp` 交给用户自行决定：保留/删除/重写
- 后续所有阶段改为 Ask + Coach 模式，不再直接写代码文件

## 22:13 今日收工
- 用户选择删除我生成的 `Core/` 目录，明天自己手写
- 收尾清单已给出：删 Core/ → 检查 git status → 2 个 commit → push → 关 Editor/VS
- 今日里程碑：**UE 5.4 C++ 项目壳可打开 Editor，模块加载正常**

## 明日接续点
- 打开 `MTBRacing.uproject` + `MTBRacing.sln`
- 用户说"继续 MTB 项目"后，我读 memory 恢复上下文
- Phase 1 第 2 步：指导用户手写 `Core/MTBGameMode.h/cpp`（已删，从零开始）
  - 要讲的关键点：UCLASS 宏、GENERATED_BODY、MTBRACING_API 宏、`.generated.h` 必须最后 include 的原理、include 路径用 `Core/MTBGameMode.h` 相对模块根
- Phase 1 第 3 步：启用 ChaosVehicles 插件 → `AMTBVehiclePawn` 骨架（第一次真碰载具系统）
