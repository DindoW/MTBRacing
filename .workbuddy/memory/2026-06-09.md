# 2026-06-09

## Phase 1 收尾：手感调参完成

用户报告 1.7 手感调参完成。

**采用方案**：山地版基线（平衡型）

| 参数 | C++ 默认值 | BP_CubeBike 实例值 |
|---|---|---|
| MaxSpeed | 2000 | 2000 |
| Acceleration | 1500 | **1800** |
| Deceleration | 1200 | **1500** |
| TurnSpeed | 120 | **140** |

**用户明确决策**：数值只保存在 BP_CubeBike 实例，C++ 默认值不动。理由是 BP 实例覆盖即可，不污染 C++ 基类参数（保持 C++ 作为最小基线）。

## 文档同步

- `docs/DEV_PLAN.md`：
  - 1.7 勾选 → Phase 1 标记完成 ✅
  - "调参点"表格增加"BP_CubeBike 实例值"列，写入山地版基线
- `MEMORY.md`：
  - "最后更新"改为 2026-06-09
  - 进度：Phase 1 整体勾选完成，新增"当前节点：阶段性提交 Phase 1 成果到 GitHub"
  - 新增"手感调参基线（山地版）"章节
  - 关键决策记录补 06-09 条目

## 下一步：Git 提交 Phase 1 成果

用户选择先把当前进度提交到 GitHub 再进 Phase 2。建议提交内容：

1. **代码层**（Source/）：
   - BikePawn.h/cpp（重写为最简 APawn + FloatingPawnMovement）
   - BikeFrontWheel / BikeRearWheel 删除
   - MTBRacing.Build.cs（去 ChaosVehicles 依赖）
   - MTBGameMode.cpp（DefaultPawnClass = ABikePawn）
2. **资产层**（Content/）：
   - Blueprints/BP_CubeBike.uasset（含手感调参后的实例值）
   - Blueprints/BP_MTBGameMode.uasset
   - Input/IMC_Bike + 3 个 IA
   - Levels/L_TestTrack.umap
3. **文档层**：
   - docs/DEV_PLAN.md（v2）
   - .workbuddy/memory/* （MEMORY.md + 日志）

**建议 commit message**：
```
feat(pawn): Phase 1 完成 - 最简 APawn + FloatingPawnMovement 跑通

- 重构 BikePawn 为 APawn 派生（去掉 AWheeledVehiclePawn 依赖）
- 移除 ChaosVehicles 模块依赖与 BikeFrontWheel/RearWheel 占位类
- 接入 Enhanced Input（IMC_Bike + Pedaling/Steering/Handbrake）
- 山地版手感基线落地在 BP_CubeBike 实例
- 更新 DEV_PLAN v2：分阶段验证策略，Chaos Vehicle 推到 Phase 6
```

**待确认**：Vehicles/SportsCar 和 MotoInteractionAnims 这两个未使用的备用资产目录要不要进 commit。如果体积大可以考虑：
- 留本地不提交（.gitignore 排除）
- 或者单独一个 commit "chore: 备用车辆资产入库"
